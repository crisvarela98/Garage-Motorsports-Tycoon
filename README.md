# Garage-Motorsports-Tycoon

## Setup

1. Copy `.env.example` to `.env`
2. Fill in `MONGODB_URI`, `MONGODB_DB_NAME`, `INSTAGRAM_CLIENT_ID`, `INSTAGRAM_CLIENT_SECRET`, and optionally `INSTAGRAM_REDIRECT_URI`
3. Run `npm install` and start the server with `node server/server.js`
